node_modules/
.vercel/
.env
.env.local
.DS_Store
*.log

# Local data files (Vercel KV is used in production)
data/
.vercel
.env*

# The leak checker word list — local only, never published.
# See the note at the top of scripts/check-leak.mjs.
.leakwords.json
